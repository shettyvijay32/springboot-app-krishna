apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: testapp
  namespace: testapp
spec:
  hosts:
  - "*"
  gateways:
  - testapp-internal
  http:
  - match:    
    - uri:
        prefix: /
    route:
    - destination:
        host: testapp.testapp.svc.cluster.local
